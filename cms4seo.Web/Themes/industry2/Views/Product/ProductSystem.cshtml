
@model IEnumerable<Category>


<div class="row mt-3">
    @foreach (var category in Model)
    {
        foreach (var item in category.GetAllChildProduct().Take(30))
        {
            @Html.Partial("_CardProduct", item)
        }
    
    }
</div>
