

<ul class="breadcrumb-style-1">
    <li>
        <a href="/" target="_top">
            <span class="fa fa-home"></span>
            <span @CMS.Attribute("Breadcrumb.Home")>
                @Setting.Contents["Breadcrumb.Home"]
            </span>
        </a>
    </li>

    @if (Model.Category != null)
    {
        if (Model.Category.ParentId != null)
        {
            if (Model.Category.Parent.ParentId != null)
            {
                <li>
                    <a href="@Url.Action("ListBy", "Product", new {slug = Model.Category.Parent.Parent.Slug})">
                        @Model.Category.Parent.Parent.Name
                    </a>
                </li>
            }
            <li>
                <a href="@Url.Action("ListBy", "Product", new {slug = Model.Category.Parent.Slug})">
                    @Model.Category.Parent.Name
                </a>
            </li>
        }
        <li>
            <a href="@Url.Action("ListBy", "Product", new {slug = Model.Category.Slug})"
               class="current">
                @Model.Category.Name
            </a>
        </li>
    }

</ul>

